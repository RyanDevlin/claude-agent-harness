# Claude Agent Harness Configuration

# Remote git repo URL (SSH or HTTPS)
REPO_URL=git@github.com:your-org/your-repo.git

# Branch to work on
REPO_BRANCH=main

# Path to the agent prompt file (relative to harness root)
AGENT_PROMPT_FILE=AGENT_PROMPT.md

# Claude model to use
CLAUDE_MODEL=claude-sonnet-4-20250514

# Max agent loop iterations (0 = unlimited, agent runs until no tasks remain)
MAX_ITERATIONS=0
