# Claude Agent Harness Configuration

# Remote git repo URL (SSH or HTTPS)
REPO_URL=git@github.com:your-org/your-repo.git

# Branch to work on
REPO_BRANCH=main

# Agent/planner prompt files — defaults are set in the Dockerfile:
#   AGENT_PROMPT_FILE=/harness/AGENT_PROMPT.md
#   PLANNER_PROMPT_FILE=/harness/PLANNER_PROMPT.md
# Uncomment to override (must be an absolute path inside the container):
# AGENT_PROMPT_FILE=/harness/AGENT_PROMPT.md
# PLANNER_PROMPT_FILE=/harness/PLANNER_PROMPT.md

# Claude model to use
CLAUDE_MODEL=claude-opus-4-6

# Max agent loop iterations (0 = unlimited, agent runs until no tasks remain)
MAX_ITERATIONS=0

# Minutes before a lock is considered stale (from a crashed agent)
LOCK_STALE_MINUTES=30

# Maximum retries for failed tasks (0 = no retries, task fails permanently)
MAX_TASK_RETRIES=2

# Enable validation phase after all tasks complete (true/false)
ENABLE_VALIDATION=true

# Maximum validation rounds (prevents infinite remediation loops)
MAX_VALIDATION_ROUNDS=2

# SSH private key file for git authentication (filename within ~/.ssh/)
SSH_KEY_FILE=harness_deploy_key
